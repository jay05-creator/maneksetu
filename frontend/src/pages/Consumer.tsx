import {AlertTriangle,ArrowRight,BookOpenCheck,MessageCircleWarning,ScanLine,ShieldCheck} from 'lucide-react';import {Link} from 'react-router-dom';import {t} from '../lib/i18n';import type {Language} from '../lib/types'
export default function Consumer({language}:{language:Language}){const cards=[[ShieldCheck,'Check a certification','Enter a licence or registration number and understand the result.','/verify'],[ScanLine,'Understand a mark','Learn what ISI, Hallmark and other conformity marks communicate.','/assistant'],[BookOpenCheck,'Product safety basics','Use standard-backed guidance to make more informed choices.','/assistant'],[MessageCircleWarning,'Complaint guidance','Understand what details to collect and where to raise a concern.','/assistant']] as const;return <div className="page consumer-page"><header><span className="kicker"><ShieldCheck/> Consumer assurance</span><h1>{t(language,'consumerTitle')}</h1><p className="lead">No jargon. Just clear routes to verify, understand and act.</p></header><div className="consumer-grid">{cards.map(([IconTitle,title,desc,to])=>{const Icon=IconTitle;return <Link to={to} className="consumer-card" key={title}><Icon/><h2>{title}</h2><p>{desc}</p><span>Explore <ArrowRight/></span></Link>})}</div><section className="safety-alert"><div><AlertTriangle/></div><div><span className="eyebrow">Sample safety notice</span><h2>Inspect pressure-cooker safety components</h2><p>Stop using a cooker if its safety valve, gasket or lid-locking mechanism is damaged. This educational fixture is not a current official recall.</p></div><span className="demo-pill">Demo alert</span></section></div>}

